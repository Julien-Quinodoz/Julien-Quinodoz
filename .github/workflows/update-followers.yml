name: Update Followers

on:
  schedule:
    - cron: '0 0 * * *'  # Exécute le workflow tous les jours à minuit UTC
  workflow_dispatch:  # Permet d'exécuter le workflow manuellement depuis l'interface GitHub

jobs:
  update-followers:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout the repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: pip install requests

      - name: Run script to update followers
        run: python update_followers.py

      - name: Commit and push changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "🔄 Mise à jour quotidienne des derniers followers"
          branch: main
          commit_user_name: "GitHub Actions"
          commit_user_email: "actions@github.com"
